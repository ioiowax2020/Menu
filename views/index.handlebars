<!-- search bar -->
<div class="container search-bar">
  <div class="search-bar-outer">
    <div class="search-bar-inner">
      <form action="/search">
        <h4 class="mb-2">尋找餐廳：</h4>
        <div class="input-group">
          <input type="text" name="keyword" class="form-control" placeholder="輸入餐廳、分類" aria-label="Restaurant Name..."
            value="{{keyword}}" aria-describedby="search-button">
          <div class="input-group-append">
            <button class="btn btn-major" type="submit" id="search-button"><i class="fas fa-search"></i></button>
          </div>
        </div>
      </form>
    </div>
  </div>
</div>

<!-- restaurant list -->

<div class="container mt-5" id="mainId">
  <a class="btn btn-outline-dark btn-lg m-2" href="./menu/new" role="button">Add <i class="fas fa-plus"></i></a>
  <a href="/" class="btn btn-outline-secondary" style="float: right;">Home</a>
  <div class="row">

    {{#each menus}}
    <div class="col-sm-6 col-md-4 col-12">
      <div class="card-columns">
        <a href="/menu/detail/{{this._id}}" class="text-secondary" style="text-decoration:none;">
          <div class="card mb-3">
            <img class="card-img-top" src="{{this.image}}" alt="{{this.name}}">
            <div class="card-body p-3">
              <h6 class="card-title mb-1">{{this.name}}</h6>
              <div class="restaurant-category mb-1">
                <i class="fas fa-utensils pr-2"></i> {{this.category}}
              </div>
<<<<<<< HEAD:views/index.hbs
              <div class="d-flex justify-content-between">
                <form action="./menu/detail/{{this._id}}" method="POST" class="mr-5" style="float:left">
                  <button type=" submit" class="btn btn-danger ">Detail <i class="fas fa-heart"></i></button>
                </form>

                <a class="btn btn-primary ml-2" href="./menu/{{this._id}}/edit" role="button">Edit</a>

                <form action="./menu/{{this._id}}/delete" method="POST" style="display:inline-block">
                  <button type="submit" class="btn btn-light"><i class="fas fa-trash-alt"></i></button>
                </form>
              </div>
=======
              <button type="button" class="btn btn-danger btn-sm">{{this.rating}} <i class="fas fa-star"></i></button>
>>>>>>> parent of c13b745 (feat:fix seed schema,add detail page):views/index.handlebars
            </div>
          </div>
        </a>
      </div>
    </div>
    {{/each}}
  </div>
</div>